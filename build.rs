fn main() -> Result<(), Box<dyn std::error::Error>> {
    // let config = tonic_build::configure();
    // config
    //     .build_server(true)
    //     .out_dir("src/proto")
    //     .compile(
    //         &["src/proto/uniffle.proto"],
    //         &["src/proto"],
    //     )?;
    Ok(())
}